import Link from 'next/link';
import LandingContainer from '../page-specific/LandingPage/LandingContainer';

const Home = props => (
  <div>
    <LandingContainer />
  </div>
);

export default Home;
